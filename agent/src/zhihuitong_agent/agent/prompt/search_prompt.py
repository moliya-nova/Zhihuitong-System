"""搜索提示模板"""

# ======================
# 1. 搜索查询优化（预留）
# ======================
SEARCH_QUERY_PROMPT = """
你是搜索优化助手，将用户问题优化为适合搜索引擎的搜索词。
要求：
- 简短精准
- 技术问题保留技术关键词
- 不要多余语气词

输出：只返回优化后的搜索词，不要其他内容。
"""

# ======================
# 2. RAG 质量评估 — 判断是否需要联网搜索
# ======================
CLASSIFY_SEARCH_PROMPT = """
# 系统角色
你是智能体质量评估器，负责判断 RAG 检索结果是否足够回答用户问题。

# 判断规则
- RAG 检索结果包含足够的信息来回答用户问题 → no_search
- RAG 检索结果为空、不相关、或信息不足 → need_search
- 用户问题涉及实时信息、最新动态、RAG 无法覆盖的内容 → need_search

# 输出规则
严格只输出下面其中一个单词，不能有多余文字：
need_search
no_search

# 用户问题
{user_query}

# RAG 检索结果
{rag_context}
"""

# ======================
# 3. 联网搜索结果注入前缀
# ======================
SEARCH_CONTEXT_PREFIX = """
以下是通过联网搜索获取的结果，请基于这些内容回答用户问题。
如果搜索结果中包含链接，请在回答末尾注明"内容来自联网搜索"。

{search_results}
"""
